
```
ldapsearch -H ldap://<TARGET_IP> -D '<USERNAME>' -w '<PASSWORD>' -b 'DC=<DOMAIN>,DC=<TLD>'
```

```
ldapsearch -H ldap://<target_IP> -x -D 'michael.wrightson@cicada.htb' -w 'Cicada$M6Corpb*@Lp#nZp!8' -b 'DC=cicada,DC=htb'
```

