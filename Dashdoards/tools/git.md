
```
echo "# Offensive_python_tools" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/hiiamyash/Offensive_python_tools.git
git push -u origin main
```

```
git remote add origin https://github.com/hiiamyash/Offensive_python_tools.git
git branch -M main
git push -u origin main
```


```
git config --global pull.rebase false # Only run this once to set a global preference
git pull origin main
# (If conflicts arise, resolve them: edit files, git add, git commit)
git push -u origin main

git pull origin main --allow-unrelated-histories


```

